Le CTRL + C doit faire comme bash 
- Afficher "^C" (FAIT)
- Supprimer le message en cours

- tglory :
  fix les leaks avec CTRL C puis plusieurs commandes inconnu
  fix: unset OLDPWD && cd .. && exit -> free(): invalid pointer
  TODO add mechanic: echo '$USER' should print $USER and not tglory
